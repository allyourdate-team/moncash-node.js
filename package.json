{
  "name": "moncash",
  "version": "0.0.1",
  "description": "nodejs moncash API wrapper",
  "main": "index.js",
  "scripts": {
    "test": "jest --coverage --testTimeout 100000",
    "coveralls": "jest --coverage && cat ./coverage/lcov.info | coveralls"
  },
  "keywords": [
    "moncash",
    "digicel",
    "API",
    "payment processing"
  ],
  "author": "Freedy Meritus <freedy@allyourdate.com>",
  "license": "MIT",
  "dependencies": {
    "axios": "^0.21.1"
  },
  "jest": {
    "testEnvironment": "node"
  },
  "devDependencies": {
    "coveralls": "^3.1.0",
    "jest": "^26.6.3"
  }
}
